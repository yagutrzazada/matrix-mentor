spring:
  application:
    name: matrix-mentor
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/x_company
    username : postgres
    password: mysecurepassword
    driver-class-name: org.postgresql.Driver
server:
  port: 8080